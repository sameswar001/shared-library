@Library('shared-library') _

node {
        stage('Example'){
            echo 'hello world'
        }
      stage('say hello'){
          sayHello 'Dave'
        }
        
       stage('Calculate'){
         calculator.add(50,55)
        }
}
