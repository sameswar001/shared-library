@Library('shared-library') _

pipeline {
    agent any
    stages{
        stage('Example'){
            sh 'echo hello world'
        }
      stage('Demo'){
          echo 'Hello World'
          sayHello 'Dave'
          }
    }
}
