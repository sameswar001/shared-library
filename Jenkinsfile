@Library('shared-library') _

pipeline {
    agent any
    stages{
        stage('Example'){
            echo 'hello world'
        }
      stage('Demo'){
          echo 'Hello World'
          sayHello 'Dave'
          }
    }
}
